{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Libreta's main window. Beyond the core API the page gets: native Save and Open dialogs; file access ONLY inside paths the user picked in those dialogs (a save target, or the workspace folder \u2014 remembered across launches by persisted-scope); and opening web links in the system browser. No shell, no arbitrary paths.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:app:allow-version",
    "dialog:allow-save",
    "dialog:allow-open",
    "fs:allow-write-file",
    "fs:allow-write-text-file",
    "fs:allow-read-file",
    "fs:allow-read-text-file",
    "fs:allow-read-dir",
    "fs:allow-mkdir",
    "fs:allow-remove",
    "fs:allow-exists",
    "fs:allow-rename",
    "opener:default"
  ]
}
