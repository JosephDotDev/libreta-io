{
  "name": "libreta",
  "version": "1.0.2",
  "private": true,
  "description": "Libreta \u2014 a local-first personal workspace. The web app itself has no build step; this package only pulls in the Tauri CLI that wraps it into a desktop app.",
  "license": "MIT",
  "scripts": {
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
